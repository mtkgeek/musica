name: flutter_audio_query
description: Flutter plugin to query data about artists, albums, songs, genres and playlists
  from device storage.
version: 0.2.1
author: Marcos Antonio Boaventura <scavenger.gnu@gmail.com>
homepage: https://github.com/sc4v3ng3r/flutter_audio_query

environment:
  sdk: ">=2.1.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  hive: ^1.0.0
  hive_flutter: ^0.2.1
  path_provider: ^1.3.0
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  hive_generator: ^0.5.1
  build_runner:


flutter:

  plugin:
    androidPackage: boaventura.com.devel.br.flutteraudioquery
    pluginClass: FlutterAudioQueryPlugin
